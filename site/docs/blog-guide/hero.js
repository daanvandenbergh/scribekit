// Hero params for blog-guide. Rendered to a JPEG by the /scribekit-hero skill; edit and re-run
// `regenerate-docs-heroes`. Single-language docs, so a plain params object (no locale function).
export default {
    gradient: "soft-sweep",
    title: "Blog recipes",
    subtitle: "Add posts, wire the routes, filter categories, override the copy.",
};
