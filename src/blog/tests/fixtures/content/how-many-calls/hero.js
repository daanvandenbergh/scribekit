// Sidecar hero-params file beside post.mdx. Not a post: entries() must ignore it
// (its stem is neither `post` nor a configured locale code), so it never becomes a slug.
export default { gradient: "veil" };
